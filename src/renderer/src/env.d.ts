/// <reference types="vite/client" />

/**
 * Replaced at build time with the version from package.json (see
 * electron.vite.config.ts / vite.preview.config.ts), so the browser preview
 * reports the same version as the desktop app.
 */
declare const __APP_VERSION__: string
